export const metadata = {
  robots: {
    index: false,
    follow: true,
  },
};

export default function ListYourPropertyLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
